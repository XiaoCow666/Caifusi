# 阶段 2 改进方案：前端 api.js HTTP 请求封装统一收敛

**问题编号**：STAGE2-ISSUE-001
**对应问题**：`src/services/api.js` 两条 HTTP 请求路径并存（axios.create 实例主力 / sendMessageToCoach 内联 fetch 绕过 axios）+ fetchApi 原生 fetch 封装死代码（全仓 0 调用、未导出）
**分析日期**：2026-09-02
**负责人**：待分配
**预计工期**：1~2 个工作日
**风险等级**：低（仅改动 1 个服务层文件，不涉及 UI / 后端路由）
**状态**：已实施（PR #3 已开，待复审）

---

## 结论与推荐（TL;DR，3~5 句）

> **结论**：正式 main（XiaoCow666/Caifusi@930da2c）的 `src/services/api.js` 中，聊天请求 sendMessageToCoach 用**内联 fetch** 绕过 axios 实例（自行拼接 `localhost + /api/coach/chat`），并存 **fetchApi 原生 fetch 封装死代码 74 行**（全仓 0 调用、未导出）——两条请求路径加一份死代码，带来部署一致性风险与双倍维护成本。
> **证据**：`git grep fetchApi` 仅命中定义行 L51、0 调用点；axios baseURL 已含 /api（L8-L11）；聊天唯一调用方 CoachChat.js#L216 全程 0 改动。
> **推荐方案（方案 2，已实施为 [PR #3](https://github.com/XiaoCow666/Caifusi/pull/3)）**：4 个 Conventional Commits——① sendMessageToCoach 迁移为 `api.post('/coach/chat')` 并保留 `result.status === 'success'` 业务包络判断（不以 HTTP 200 判定成功）；② 删除 fetchApi 死代码 74 行；③ api.js 顶部新增请求工具使用规范注释；④ 评审修正：规范注释示例不得 import 未导出的 api 实例（commit `c8017ea`）。
> **验收结果**：每个 commit 均 `npm run build` 通过；单文件 68+/114-，净删 46 行 ≥ 40 达标；静态验收 A1~A7 全部满足，B1~B3 运行时回归待本地后端执行（详见 §5.1）。
> **风险与回滚**：低——仅 api.js 单文件改动、函数签名与返回值不变、错误文案 1:1 对齐；异常时按 §5.3 两条命令即可回滚。

## 一、问题背景与现状证据

### 1.1 问题名称（2026-09-05 按正式 main = XiaoCow666/Caifusi@930da2c 重述）

前端请求服务层 `src/services/api.js` 中存在两条并行的 HTTP 请求路径：① 主力 axios.create 实例（统一 baseURL = `http://localhost:5001/api`——**已含 /api** + JWT 请求拦截器）；② AI 教练聊天请求 sendMessageToCoach 绕开 axios 实例，在函数内部内联 fetch 并自行拼接 `${baseUrl}/api/coach/chat`（[L185-L235](src/services/api.js#L185-L235)）。另有 fetchApi 原生 fetch 封装（[L50-L123](src/services/api.js#L50-L123)，74 行）为**全仓 0 调用死代码且未导出**。聊天请求脱离 axios 的 baseURL 与拦截器体系，与其它业务函数行为不一致，造成维护成本与部署一致性风险。

### 1.2 问题发现路径
本问题来自《项目评审.md》修订版 §五 P2-3「前端请求层多套实现并存、fetchApi 疑似死代码」的目录梳理结论（原评审 §2.2 验证「网络超时/失败重试」表述时，顺带发现不同业务函数调用的请求工具不一致，展开静态代码审计后确认三套并存的结构）。随后通过以下 3 种独立方法交叉验证：① VS Code `Ctrl+F` 单文件逐关键词定位实现锚点；② Windows PowerShell 正则全仓搜索「真调用」匹配数；③ VS Code 右键「查找所有引用」语义分析引用数量。（注：该"0 调用死代码"结论对正式 main = XiaoCow666/Caifusi@930da2c 成立；本工作区曾基于演进版代码一度得出"单点调用"的相反结论，最终以正式 main 为准，见 §1.3。）

### 1.3 现状证据（2026-09-05 按正式 main = XiaoCow666/Caifusi，HEAD 930da2c 重新核对）

a) 两条请求路径的代码锚点（正式 main，`src/services/api.js`）：
   - 路径 1（主力）：axios.create 实例 api
     配置：[api.js#L14-L19](src/services/api.js#L14-L19)；baseURL = API_BASE_URL（[L8-L11](src/services/api.js#L8-L11)），本地开发 = `http://localhost:5001/api`——**已含 /api**，故各业务函数路径均不带 /api（如 `/auth/login`、`/coach/chat`）
     请求拦截器加 JWT：[api.js#L22-L33](src/services/api.js#L22-L33)；响应拦截器处理 401 登出：[api.js#L36-L48](src/services/api.js#L36-L48)（当前无超时/重试机制）
   - 路径 2（聊天专用，绕过 axios）：
     - sendMessageToCoach（唯一业务入口，调用方 [CoachChat.js#L216](src/pages/CoachChat.js#L216)）：[api.js#L185-L235](src/services/api.js#L185-L235)（51 行），函数内部内联 fetch，自行拼接 `${baseUrl}/api/coach/chat`（[L200](src/services/api.js#L200) 硬编码完整路径），并在 [L221](src/services/api.js#L221) 校验 `result.status === 'success'`
     - fetchApi 原生 fetch 封装（**死代码**）：[api.js#L50-L123](src/services/api.js#L50-L123)（74 行含归属注释），dev 地址在函数内部硬编码（[L62](src/services/api.js#L62)），并自行拼接 `/api` 前缀（[L67](src/services/api.js#L67)）；未 export、无内部调用
b) 「fetchApi 死代码」结论（正式 main 实测）：
   验证命令（仓库根目录执行）：
   ```bash
   git grep -n "fetchApi" HEAD -- src
   ```
   实际输出（仅 1 行 = 仅定义行）：
   ```
   src/services/api.js:51:async function fetchApi(endpoint, options = {}) {
   ```
   结论：**fetchApi 为全仓 0 调用死代码**——sendMessageToCoach 是内联 fetch，不经过 fetchApi；函数也未 export，外部文件无法 import（构建期即报错）。旧方案稿中「4 个文件 9 匹配、清理 3~6 处 import」等描述属夸大，实为单文件内单一定义。
c) 双路径并存的可视化影响：
   - 聊天请求 URL 不走 axios 实例的 baseURL：sendMessageToCoach 内部拼 `http://localhost:5001` + `/api/coach/chat`（L191-L200）→ 改 axios 的 baseURL 配置（如部署 GitHub Pages / 换 REACT_APP_API_URL）时，聊天请求不会跟随，其余接口会
   - 死代码与重复实现并存：fetchApi 74 行（0 调用）+ sendMessageToCoach 内联 fetch 51 行，均与 axios 体系重复造轮子
d) 运行状态说明：本结论基于静态代码证据（git grep + 逐函数引用核查），未端到端跑 UI 验证（聊天接口在无智谱 API Key 时会返回业务错误，但与请求路径归属的判断无关）。

### 1.4 问题为何未及时发现
(1) **迭代多人分工无 PR 规范**：三套代码分属不同迭代阶段，由不同协同学独立编写（推测第 1 套 = 登录模块、第 2 套 = 通用服务层重构草稿、第 3 套 = AI 教练功能上线时独立开发），合并时缺少「所有 HTTP 请求必须统一走 axios.create 实例」的 Review Checklist。
(2) **属于维护性问题而非用户功能缺陷**：页面 UI 交互一切正常（登录、测评提交、聊天发送在前端侧均能正常发请求），仅在后续叠加"统一超时、统一 JWT 头、统一错误埋点"等横切需求时才会暴露不一致性，常规黑盒功能测试无法发现。
(3) **项目未接入静态代码分析工具链**：
   - 未启用 ESLint `no-unused-vars` + `import/no-unused-modules` 规则
   - 未周期性运行 `npx depcheck` 扫描未使用函数 / 未使用依赖
   导致「聊天请求绕开 axios 实例内联 fetch、fetchApi 死代码（74 行）长期留存」的横切维护问题长期未被自动告警。

---

## 二、问题影响和改进目标

### 2.1 问题影响（分层描述，仅保留已验证或高概率可复现的真实影响）

| 影响维度 | 具体表现 | 严重程度 | 代码例证 |
|---|---|---|---|
| 🛠 可维护性（最高） | 后续叠加「统一加 Authorization Header、统一错误码中文翻译、统一埋点上报、统一超时」等横切功能时，axios 实例（路径 1）改了聊天路径（路径 2）不跟随，反之亦然——漏改直接导致用户体验不一致。典型场景：axios 侧统一了错误文案，聊天请求仍走自己的 try/catch 文案；两条路径行为永远差半拍。 | 🟠 中（短期不影响功能，长期维护成本按双路径翻倍） | [api.js#L14-L19 axios 配置](src/services/api.js#L14-L19) vs [api.js#L185-L235 sendMessageToCoach](src/services/api.js#L185-L235) |
| 🚀 上线一致性风险（较高） | 聊天请求在 sendMessageToCoach 内部自拼 dev 地址 `http://localhost:5001` + `/api/coach/chat`（[L191-L200](src/services/api.js#L191-L200)），未复用 axios 实例的 baseURL（axios 侧已支持 REACT_APP_API_URL，[L9-L11](src/services/api.js#L9-L11)）：当部署到 GitHub Pages `https://xiaocow666.github.io/Caifusi/` 或其他非 localhost 环境时，聊天接口会因跨域（CORS）或目标地址错误直接 404 / Network Error，其他接口正常 → 造成"部分功能能用部分不能用"的偶发线上 Bug，排查定位困难。 | 🟠 中（本地开发不触发，生产部署必现） | sendMessageToCoach 内联硬编码（L200）vs axios API_BASE_URL（L9-L11） |
| 🧹 代码整洁度 / 新人上手 | fetchApi 死代码 74 行（0 调用、未导出）+ sendMessageToCoach 内联 fetch 51 行与 axios 体系并存，且无使用规范注释，新成员难以判断"项目推荐用哪种方式发请求"，拉长 Onboarding 时间。 | 🟡 低（主要影响新人上手效率） | `git grep fetchApi` 命中 1 行（仅定义 L51） |
| 🔒 安全性 | ❌ 无新增风险：fetchApi 未导出、0 调用，无外部攻击面；内联 fetch 与 axios 在 TLS / CSRF / XSS 防护能力上等价，未引入新漏洞（评估中已剔除"隐藏后门"类臆测描述）。 | ⚪ 无 | —— |

### 2.2 改进目标（严格 SMART 原则 + 可量化验收）

> **SMART 目标声明**：在 5 个工作日内（截止 2026-09-07），将前端 `src/services/api.js` 中两条 HTTP 请求路径 + 一份 fetchApi 死代码**收敛为 1 套（仅保留 axios.create 实例）**，达成以下可量化指标：
>
> | 编号 | 指标项 | 可量化标准 | 验证方式 |
> |---|---|---|---|
> | G1 | 代码减量 | 删除 fetchApi 死代码 **74 行**（[L50-L123](src/services/api.js#L50-L123) 含归属注释，Commit 2 单 commit 纯删除），**整体净删除 ≥ 40 行（PR #3 实测：68 insertions / 114 deletions = 净删 46 行，含 commit 4 `c8017ea` 评审修正）**；单文件完成（无外围 import 可清理） | `git diff --stat main...HEAD` 输出中 `insertions(+), deletions(-)` 统计 |
> | G2 | 实现统一 | sendMessageToCoach 函数 100% 重写为 `await api.post('/coach/chat', data)` 调用第 1 套 axios 实例（baseURL 已含 `/api`，路径不能再拼 `/api`），不再出现原生 `fetch(` 关键字 | api.js 内 Ctrl+F 搜 `fetch(` → 匹配数从 2 降为 0（不含注释） |
> | G3 | 功能等价回归 | 3 条核心链路在 Chrome DevTools Network 面板中：请求 URL（域名 + path）、Method、Content-Type、Body JSON 结构与重构前**字节级一致**；**业务包络判断保留：仅当 `response.data.status === 'success'` 才算成功，不能仅凭 HTTP 200**（后端响应为 success/error 包络）；相同输入（密码错误 / 空聊天消息）下错误提示文案语义一致（以后端响应体中文 message 为准） | 手工对比 Network 面板截图 + Console 错误消息 |
> | G4 | 构建通过 | `npm run build` 成功生成 `build/` 产物，Babel / Webpack 编译错误数 = 0（当前项目未接入 ESLint，则以编译通过为最低标准） | `npm run build` exit code = 0 |
> | G5 | 新人上手可理解性 | api.js 文件头部新增「请求工具使用规范注释」，协同学仅阅读注释即可知道「所有 HTTP 必须使用 api 实例、禁止新写原生 fetch、baseURL 配置位置」，10 分钟内可独立完成新接口调用的代码编写（模拟 Code Review 验证：给新人一个接口文档，看其能否正确写出调用） | 结对编程 / 模拟评审 |

---

## 三、可选方案及取舍

共设计 3 套方案（方案 0 = 什么都不做作为 Baseline 对比），从工作量、风险、达成目标覆盖度 3 维度打分对比：

### 3.1 方案对比总表

| 维度 | 方案 0：**不做任何修改（Baseline）** | 方案 1：**收敛代码**（sendMessageToCoach 迁移 axios + 删除 fetchApi 死代码，不加规范注释） | 方案 2：**收敛 + 规范注释**（方案 1 + api.js 顶部加使用规范注释）【推荐方案】 | 方案 3：**重写 api.js 为 TypeScript 类封装（Over-engineering）** |
|---|---|---|---|---|
| **核心动作** | 保持现状，仅在评审文档里记录该问题 | 3 步：① sendMessageToCoach 改写为 `api.post('/coach/chat')`（baseURL 已含 /api，保留 `result.status === 'success'` 包络判断）；② 删除 fetchApi 死代码 74 行（0 调用、未导出，可独立删除）；③ 跑 `npm run build` 验证 | 方案 1 全部动作 + ④ api.js 顶部加使用规范注释 | 引入 TypeScript + 抽象 `BaseHttpClient` 类 + 拦截器 + 重试中间件，整体重写 |
| **涉及文件数** | 0 | 1 个（仅 api.js；fetchApi 未导出、外部文件 0 引用，无 import 可清理） | 同方案 1 | 全文件重命名 .ts + 新 types/http.ts 目录，≥ 10 个文件 |
| **代码变更量** | 0 行 | -114 行 删除 + 约 +24 行 新增（迁移重写）= 净删除 ≈ -90 行 | 方案 1 基础上再 +41 行规范注释 = **净删除 ≈ -49 行（实测：65+/114-）** | +500 ~ +800 行 大量新增 |
| **预计工期** | 0 | 约 4h（含回归验证） | 约 5h（含回归验证） | 5~7 天 |
| **风险等级** | 0（但问题持续存在） | 🟢 低（仅 api.js 内函数级替换 + 删除，签名与返回值结构不变，调用方 CoachChat.js#L216 0 改动） | 🟢 低（同方案 1） | 🔴 高（全项目类型体操 + 构建链迁移 + 运行时回归面大） |
| **目标 G1 代码减量** | ❌ 0 行 → 不达标 | ✅ ≥ 40 行 → 达标 | ✅ ≥ 40 行 → 达标 | ❌ 净增行 → 不达标 |
| **目标 G2 实现统一（无 fetch 关键字）** | ❌ 双路径 + 死代码并存 → 不达标 | ✅ 仅 1 套（axios）→ 达标 | ✅ 仅 1 套 → 达标 | ✅ 仅 1 套 → 达标 |
| **目标 G3 功能等价** | ✅（天然不变） | ⚠️ 需回归聊天路径（包络判断 + 400/500 文案语义，可控） | ⚠️ 同方案 1 | ⚠️ 需完整端到端回归（拦截器行为变化风险） |
| **解决 §2.1「上线一致性风险」**（最关键痛点） | ❌ 未解决 | ✅ 聊天请求并入 axios baseURL（上游已支持 REACT_APP_API_URL，L9-L11）→ 部署到 GitHub Pages 自动适配环境 | ✅ 同方案 1 | ✅ 解决 |
| **新人上手（G5）** | ❌ 无规范注释 | ❌ 无规范注释，双路径虽已收敛但历史背景无记录 | ✅ 规范注释明确唯一用法 → 达标 | ✅ 规范最强，但 TS 门槛反加重新人负担 |
| **综合评分（10 分制，越高越推荐）** | 2 分（问题保留，不作为方案） | 7 分（代码收敛目标全部达成，仅差 30min 注释补齐 G5） | **9 分（推荐方案）** — 全目标覆盖 + 解决上线一致性核心痛点，风险可控 | 4 分（过度设计，工期与风险不成比例，建议阶段 3 再考虑） |

### 3.2 方案 1 vs 方案 2 取舍理由（按正式 main = XiaoCow666/Caifusi@930da2c 核对）

正式 main 上 fetchApi 与 sendMessageToCoach **互不依赖**（fetchApi 为 0 调用死代码、sendMessageToCoach 是内联 fetch），删除 fetchApi 可独立执行；但为让 Reviewer 能分别评审「行为改动」（迁移）与「纯删除」（死代码），仍按 **Commit 1 迁移 → Commit 2 删除 → Commit 3 注释** 的顺序提交（与 PR #3 一致）。方案 1 与方案 2 的代码动作差异仅在注释一份：

1. **fetchApi 删除零风险可独立**：74 行死代码、未导出、无调用，删错可单 commit revert——删除动作不依赖迁移，两方案风险面一致（均限 api.js 单文件）。
2. **sendMessageToCoach 改写风险为 0**：函数签名保持 `async (data) => { return { reply }; }` 不变，调用方 [CoachChat.js#L216](src/pages/CoachChat.js#L216) 0 行改动。改写前后请求的 URL / Method / Body 结构完全等价，仅底层从「内联 fetch + 手动 json 解析」切换到「axios.create + 自动 json 解析」；**业务包络判断（`result.status === 'success'`，旧实现 L221 已有）与错误文案按 §4.3 Commit 1 模板 1:1 保留**，错误处理分支行为一致（均 reject Promise，被上层 try/catch 捕获）。
3. **30min 注释换 G5 达标，边际收益明确**：不加注释，新人仍可能再写出第二条 fetch 路径（死代码与内联 fetch 正是这么产生的）；加注释 + 历史背景记录后，"所有 HTTP 必须走 api 实例"成为文件内可见的硬约束，避免技术债二次堆积。

**结论：直接推荐方案 2（收敛代码 + 规范注释）。**

---

## 四、推荐方案与具体实施步骤

**推荐：方案 2（全量收敛）**
预期工期：1 个工作日（8h，按 2h 准备 + 2h 编码 + 2h 验证 + 2h 文档/PR 编写估算）。

### 4.1 Git 操作规范（提交前必做）

| 规范项 | 要求 |
|---|---|
| 分支命名 | `refactor/api-request-unify`（type(scope): 语义化） |
| 分支基础 | 必须从最新 `origin/main` 切出，不能基于旧 commit |
| Commit 粒度（4 个独立 commit：§4.3 三个实施步 + 评审修正 commit 4 `c8017ea`，便于 Code Review + 回滚） | 见 4.3 步骤中每个「Commit X」的规范标题（使用 Conventional Commits 格式） |
| PR 标题 | `refactor(api): 聊天请求统一收敛到 axios 实例，删除 fetchApi 原生 fetch 死代码` |
| PR 描述模板 | 引用本方案文档 §1、§2 关键证据 + §5 验收清单勾选框 |

### 4.2 环境准备（Step 0，必做，预计 1h）

```powershell
# S0-1 切到项目根目录
cd "d:\code_project\CaiFuSi"

# S0-2 拉取最新 main，确保基准正确
git checkout main
git pull origin main

# S0-3 切独立功能分支（严格按命名规范）
git checkout -b refactor/api-request-unify

# S0-4 安装前端依赖（首次跑需，后续跳过）
npm install

# S0-5 跑一次 build 验证「改之前代码是能编过的」，建立基线
npm run build
# 预期输出：Compiled successfully，build 目录生成
# ⚠️ 如这一步 build 失败，先向组内确认是否已知问题，不要继续
```

### 4.3 分步实施 + 代码级改动说明（预计 2.5h）

**Commit 1：sendMessageToCoach 迁移到 axios 实例（行为改动单独评审；= PR #3 commit a15d154）**
预计 1h，Commit 标题：
> `refactor(api): sendMessageToCoach 改用 axios.create 实例，保留 success 包络判断`

```
Step 1-1：定位 sendMessageToCoach 函数
   - [api.js#L185-L235](src/services/api.js#L185-L235)（正式 main 930da2c）；调用方 [CoachChat.js#L216](src/pages/CoachChat.js#L216)，本步骤不改调用方

Step 1-2：将整个函数体替换为如下实现（保留签名 async (data) => { return { reply } } 不变）
   ———— 以下为替换后的完整函数（= PR #3 合入版，复制粘贴即可）————

   export const sendMessageToCoach = async (data) => {
     try {
       // 统一走 axios.create 实例 api：自动继承 baseURL（已含 /api，路径不再拼 /api）、拦截器等全局配置
       const response = await api.post('/coach/chat', data);
       const result = response.data;

       // 后端返回 success/error 业务包络：不能仅凭 HTTP 200 判定成功
       if (result.status === 'success') {
         return { reply: result.reply };
       }

       // HTTP 200 但业务包络为 error（防御未来网关吞错 / 接口变更）
       throw new Error(result.message || '获取回复失败');
     } catch (error) {
       console.error('AI教练请求错误:', error);

       // 优先取后端 4xx/5xx 响应体中的中文 message（axios 错误对象自带 error.response），兜底取错误信息
       const errorMsg =
         (error.response && error.response.data && error.response.data.message) ||
         error.message ||
         'AI教练暂时无法回复，请稍后再试';

       // 网络错误 / 后端未启动：与旧实现文案语义一致
       if (
         errorMsg.includes('无法连接到服务器') ||
         errorMsg === 'Failed to fetch' ||
         errorMsg === 'Network Error'
       ) {
         throw new Error('无法连接到AI教练服务，请确认后端服务已启动');
       }

       throw new Error(`AI教练回复错误: ${errorMsg}`);
     }
   };

   ———— 以上为替换后的完整函数 ————

   改动说明（写进 Code Review 评论）：
   a) 旧实现内联 fetch 硬编码 `${baseUrl}/api/coach/chat`（L191-L200，dev 兜底 localhost:5001）→ 改为 api.post('/coach/chat')，统一走 axios baseURL（已含 /api，若写成 '/api/coach/chat' 会得到 /api/api/coach/chat）；部署环境只需改 axios 的 baseURL 一处（API_BASE_URL，L8-L11，已支持 REACT_APP_API_URL）
   b) **保留业务包络判断 result.status === 'success'（旧实现 L221 已有，不能仅以 HTTP 200 判定成功）**；后端 400/500 带 {status:'error', message}（中文）时，axios reject 的 error.response 原样保留（上游响应拦截器 L36-L48 直接 reject、无包装），catch 取 response.data.message 1:1 对齐，无需额外状态码翻译表
   c) 返回值结构 100% 保持 { reply }，调用方 CoachChat.js#L216 0 行改动，解耦修改面

Step 1-3：验证
   - 运行时回归（后端已启动）：聊天发送成功 → Network 面板 URL = axios.baseURL + '/coach/chat'，回复正常；mock {status:'error'} 响应验证包络分支会抛错（对应验收 B3）
   - api.js 内搜 `fetch(` → 本步后 fetchApi 仍在（死代码），命中 1 处属正常，Commit 2 完成后才归 0（G2 最终判定）

Step 1-4：做第 1 个 Commit（严格按 Conventional Commits）
   git add src/services/api.js
   git commit -m "refactor(api): sendMessageToCoach 改用 axios.create 实例，保留 success 包络判断

- 聊天请求从内联 fetch（硬编码 baseUrl + '/api/coach/chat'）迁移到 api.post('/coach/chat', data)
- api 实例 baseURL 已含 /api，路径不再拼 /api（避免 /api/api/coach/chat）
- 保留 result.status === 'success' 业务包络判断：不依赖 HTTP 200 判定成功
- 错误文案优先取后端响应体中文 message，网络错误文案与旧实现语义一致
- 函数签名与返回值结构不变，调用方 CoachChat.js#L216 0 改动
- Issue: STAGE2-ISSUE-001"
```

---

**Commit 2：删除 fetchApi 原生 fetch 封装（死代码，可独立删除；= PR #3 commit 8988d06）**
预计 30 min，Commit 标题：
> `refactor(api): 删除 fetchApi 原生 fetch 封装（全仓 0 调用死代码）`

```
Step 2-1：删除范围（正式 main 930da2c 行号）
   - fetchApi 函数定义 + 归属注释：[api.js#L50-L123](src/services/api.js#L50-L123)（74 行，`// 通用请求函数` 注释起，函数闭合 `}` 止）
   - 前置核对（删除前必跑）：`git grep -n "fetchApi" HEAD -- src` → 应仅 1 行（定义行 L51），无内部调用；fetchApi 未 export，外部文件无法引用
   ⚠️ 仅删除 fetchApi 自身，注意不要误删上方拦截器代码（L36-L48）与下方 registerUser（L125 起）

Step 2-2：验证
   - api.js 内 Ctrl+F 搜 `fetch(`（不含注释）→ 匹配数 = 0（G2 达标判定条件！）
   - api.js 内搜 `localhost:5001` → 仅剩 API_BASE_URL 的 [L11](src/services/api.js#L11)（axios baseURL 配置来源）1 处命中，其余 0
   - `git grep -n "fetchApi" HEAD -- src` → 输出 0 行
   - `npm run build` 通过（G4 前置）

Step 2-3：做第 2 个 Commit
   git add src/services/api.js
   git commit -m "refactor(api): 删除 fetchApi 原生 fetch 封装（全仓 0 调用死代码）

- 删除 fetchApi 函数及归属注释约 74 行（正式 main 实测全仓 0 引用：函数未导出、无内部调用）
- 聊天请求已随前一 commit 迁移到 axios 实例，无残留引用
- Issue: STAGE2-ISSUE-001"
```

---

**Commit 3：api.js 顶部新增「请求工具使用规范注释」（新人上手指引，G5 达标；= PR #3 commit 8abc0b3）**
预计 30 min，Commit 标题：
> `docs(api): 新增 api.js 请求工具使用规范注释，统一新人编写约定`

```
Step 3-1：在 api.js 文件头部，[import axios](src/services/api.js#L42) 的上方，插入如下块注释（= PR #3 commit 8abc0b3 合入版；【唯一正确用法】示例已在 commit 4 `c8017ea` 按评审意见修正——api 实例未导出、外部不得 import，以下为修正后全文）：

/**
 * ============================================================
 * 财赋思前端 HTTP 请求服务层（api.js）使用规范
 * ============================================================
 *
 * 【唯一正确用法】所有 HTTP 请求必须统一走下方 axios.create() 创建的 api 实例，
 *   但该实例仅供本文件内部使用（模块私有、未 export，外部 import 会构建报错）：
 *     外部调用只能 import 本文件具名导出函数，或默认导出 apiService（文件底部已聚合全部接口）：
 *       import apiService, { sendMessageToCoach } from '../services/api';
 *       const res = await apiService.loginUser({ email, password });
 *     新增接口 = 在本文件内按下文【新增接口规范】模板新写封装函数后具名导出；
 *     下文示例中的 api.xxx() 均指本文件内部写法。
 *
 * 【强制禁止】❌ 不要在本项目任何 .js/.jsx 文件中新写原生 fetch() 或新的 fetch 封装！
 *     错误写法：fetch('http://localhost:5001/api/xxx', {...})  ← 会造成配置不同步
 *     替代写法：await api.post('/xxx', body) / await api.get('/xxx')
 *
 * 【路径规则】api 实例的 baseURL 已含 /api（见 API_BASE_URL），请求 path 一律不带 /api：
 *     api.post('/coach/chat')          ✅
 *     api.post('/api/coach/chat')      ❌ 会得到 /api/api/coach/chat
 *
 * 【业务包络】后端接口统一返回 { status: 'success' | 'error', ... } 包络，
 *     不能仅凭 HTTP 200 判定成功，须校验 response.data.status === 'success'
 *     （参考 sendMessageToCoach 的写法）。
 *
 * 【全局配置位置】公共配置统一修改下方 axios.create() 参数块与拦截器，改 1 处全局生效：
 *     - baseURL：API_BASE_URL（本地开发 = http://localhost:5001/api；GitHub Pages /
 *       自定义域名走 REACT_APP_API_URL）
 *     - JWT 认证头：请求拦截器（localStorage 读取 authToken）
 *     - 401 登出 / 统一错误文案：响应拦截器（当前仅处理 401；如需超时/重试统一加在这里）
 *
 * 【新增接口规范】新增接口调用按以下模板写（对齐 submitAssessment 风格）：
 *     export const getXxxData = async (params) => {
 *       const res = await api.get('/xxx/data', { params }); // path 不拼 /api
 *       return res.data;
 *     };
 *
 * 【历史说明】本文件曾存在 fetchApi() 原生 fetch 封装（全仓 0 调用死代码）与
 *             sendMessageToCoach 内联 fetch（硬编码 localhost + '/api/coach/chat' 路径），
 *             已于 STAGE2-ISSUE-001 PR（refactor/api-request-unify 分支）迁移删除。
 *             如发现代码中仍残留 fetch( 关键字或新写的 fetch 封装，请提 Issue 或直接发 PR
 *             迁移到 axios 实例。
 * ============================================================
 */

Step 3-2：验证
   - 新成员打开 api.js，无需阅读任何文档即可回答三个问题：
     ① 我该用什么发请求？（api 实例 / 具名封装函数）
     ② 我不能写什么？（原生 fetch / 新 fetch 封装）
     ③ 全局配置在哪改？（axios.create 参数块 + 拦截器）→ 对应 G5 达标

Step 3-3：做第 3 个 Commit
   git add src/services/api.js
   git commit -m "docs(api): 新增 api.js 请求工具使用规范注释，统一新人编写约定

- 明确 api 实例为唯一正确用法，强制禁止原生 fetch / 新 fetch 封装
- 标注路径规则（baseURL 已含 /api，path 不拼 /api）
- 注明后端 success/error 业务包络校验要求
- 标注全局配置位置（baseURL / JWT 拦截器 / 响应拦截器）
- 说明历史 fetchApi 死代码与 sendMessageToCoach 内联 fetch 的收敛背景
- Issue: STAGE2-ISSUE-001"
```

### 4.4 本地验证（Step 4，预计 1.5h）

```
Step 4-1：构建验证（G4 达标）
   cd "d:\code_project\CaiFuSi"
   npm run build
   → 预期：Compiled successfully，退出码 0，build/static 目录生成正确

Step 4-2：静态检查清单（手工 + 工具混合）
   [ ] api.js Ctrl+F 搜 `fetch(` → 匹配数 = 0（不含注释，G2）
   [ ] `git grep -n "fetchApi" HEAD -- src` → 最多命中 api.js 头部注释【历史说明】（代码引用 = 0，fetchApi 死代码 0 残留）
   [ ] api.js Ctrl+F 搜 `localhost:5001` → 代码中仅 API_BASE_URL（L11，axios baseURL 配置来源）1 处命中（头部规范注释中作为示例出现的 URL 不计）
   [ ] 聊天函数内无自拼 URL：`sendMessageToCoach` 内仅 `api.post('/coach/chat', data)` 一行请求调用
   [ ] api.js 头部规范注释块完整，包含「正确用法/禁止写法/路径规则/业务包络/配置位置/模板」六要素（G5）

Step 4-3：运行时功能等价回归（G3 达标，需后端已启动，参考 §0 启动）
   a) 启动后端（另一个 PowerShell 窗口）：
      cd backend; D:/ai/anaconda/python.exe run.py  → Running on :5001
   b) 启动前端开发服务器：
      npm start  → 自动打开 http://localhost:3000
   c) Chrome DevTools（F12）切到 Network 面板，勾选「Preserve log」，依次执行：
      【用例 1】登录流程（Login.js 提交）
          - 输入任意邮箱+密码点登录
          - Network 面板抓 /api/auth/login（或对应模拟接口），记录 Method/URL/Body
          - 保存截图为 before-commit-1-login.png；与重构前同一操作的截图对比字节级一致
      【用例 2】测评提交（Assessment.js 提交）
          - 随机填完 10 道题点提交
          - Network 抓 /api/assessment/submit，记录 Method/URL/Body
          - 同上对比一致性
      【用例 3】AI 聊天（重点！sendMessageToCoach 改动点）
          - 进入聊天页，输入任意一句话点发送
          - Network 抓 /api/coach/chat 请求：
              ✅ URL = axios.baseURL（已含 /api）+ '/coach/chat'，最终 = http://localhost:5001/api/coach/chat（不再是 sendMessageToCoach 内联拼接的 `${baseUrl}/api/coach/chat`）
              ✅ Method = POST，Content-Type = application/json
              ✅ Request Payload JSON 结构 = { user_id, message, assessment_results, ... } 与重构前一致
              ✅ 成功响应：console 观察 result.status === 'success' 分支命中，页面正常显示 reply（不能仅凭 HTTP 200 判定成功）
              ✅ 失败响应：无智谱 API Key 时后端返回 HTTP 500 + {status:'error', message}，前端收到"AI教练回复错误: ..."类文案，与重构前语义一致
              ✅（可选）临时 mock 成 HTTP 200 + {status:'error'} 响应，验证包络分支正确抛错而非把 reply 当成功
          - 保存截图为 commit-1-chat-before-after.png
      【用例 4】Dashboard mock 数据加载
          - 进入仪表盘，抓 /api/dashboard/overview（如有真实接口调用）
          - 与重构前同一请求对比，100% 字节级一致
```

### 4.5 PR 编写与提交（Step 5，预计 1h）

```
Step 5-1：推送分支到远端 fork（正式 main 属 XiaoCow666/Caifusi，需先 fork；PR #3 实际路径：fork = fairy-song/Caifusi-1）
   git push https://github.com/fairy-song/Caifusi-1.git HEAD:refs/heads/refactor/api-request-unify

Step 5-2：GitHub 开 Pull Request（目标 XiaoCow666/Caifusi:main；PR #3 已创建：https://github.com/XiaoCow666/Caifusi/pull/3）
   - 标题：refactor(api): 聊天请求统一收敛到 axios 实例，删除 fetchApi 原生 fetch 死代码
   - Base 分支：XiaoCow666/Caifusi:main；Head 分支：fairy-song:Caifusi-1:refactor/api-request-unify
   - PR 描述模板（已落地为 PR #3 描述，复制备用）：

   ## 背景
   解决 STAGE2-ISSUE-001（评审稿《一个小问题.md》）：
   api.js 存在两条 HTTP 请求路径：axios 实例（主力，baseURL 已含 /api）与 sendMessageToCoach 内联 fetch（自行拼接 `${baseUrl}/api/coach/chat`，部署到非 localhost 环境必现跨域/404）；另有 fetchApi 原生 fetch 封装 74 行为全仓 0 调用死代码（未导出）。详见评审稿 §1。

   ## 改动清单（4 个 Commit，对应评审稿 §4.3）
   - [x] commit 1 `a15d154`：refactor(api): sendMessageToCoach 改用 axios.create 实例，保留 success 包络判断
   - [x] commit 2 `8988d06`：refactor(api): 删除 fetchApi 原生 fetch 封装（全仓 0 调用死代码，74 行纯删除）
   - [x] commit 3 `8abc0b3`：docs(api): 新增 api.js 请求工具使用规范注释，统一新人编写约定
   - [x] commit 4 `c8017ea`：fix(api): 修正顶部规范注释示例——api 实例未导出，外部不得 import { api }（评审意见闭环，纯注释行替换）

   ## 变更量统计（G1 代码减量验证）
   `git diff --stat main...HEAD`：
   Files changed: 1（api.js）| Insertions(+): 68 | Deletions(-): 114
   → 净删除 46 行 ≥ 40 ✅（其中 Commit 2 纯删除 74 行；commit 4 注释修正 +7/-4）

   ## 验收清单（对应评审稿 §5.1，口径 = api.js 代码内、不含注释）
   - [x] 构建通过：基线 + 每个 Commit 后 npm run build exit 0（共 5 次实测；commit 4 c8017ea 已重跑复核，Compiled successfully）
   - [x] fetchApi 清除：git grep fetchApi 全仓 src 代码 0 引用；api.js 内 fetch(（不含注释）= 0
   - [x] 上线一致性：api.js 代码内 localhost:5001 仅 API_BASE_URL 1 处；sendMessageToCoach 内无自拼 URL
   - [x] 规范注释示例修正：顶部示例不再 import 未导出的 api 实例（commit 4 c8017ea）
   - [x] 调用方 0 改动：CoachChat.js#L216
   - ⚠️ 范围外残留（PR diff 仍仅 api.js）：testApi.js / setupProxy.js 保留 localhost 与 fetch，见评审稿 §5.1「范围口径声明」
   - [ ] Network 回归（B1~B3，需本地后端）：登录/测评/聊天 3 条链路 + 聊天错误文案三分支

   ## 风险与回滚
   风险评估：低（仅 api.js 1 个文件改动，调用方 0 行变更）。回滚方式：评审稿 §5.3。

   ## 相关文件
   - 评审稿：一个小问题.md（§5.1 验收 A1~A7 + B1~B3 共 10 项）
   - 问题来源：项目评审.md §五 P2-3
```

---

## 五、验收标准、风险与回滚方案

### 5.1 验收标准清单（Code Review + 测试双维度）

Code Reviewer 合并本 PR 前，**必须逐项确认以下 10 条全部打勾**：

| 编号 | 类别 | 验收项 | 可复现验证命令/截图 | 通过标准 |
|---|---|---|---|---|
| A1 | 静态 | 构建通过 | `npm run build` 终端输出最后 5 行截图 | `Compiled successfully`，退出码 0 |
| A2 | 静态 | api.js 内无原生 fetch 关键字 | api.js 内 Ctrl+F 搜 `\bfetch\s*\(`（正则开启） | 匹配数 = 0（注释内提及不算） |
| A3 | 静态 | fetchApi 死代码完全清除 | `git grep -n "fetchApi" HEAD -- src` | 代码引用 = 0（PR 头注释【历史说明】中作为背景文字提及的不计入，与 A2「注释内提及不算」同理） |
| A4 | 静态 | fetchApi 未被外部文件引用 | 全局搜 `fetchApi`（含 import） | 代码匹配数 = 0（fetchApi 未 export，外部文件本无法引用，双向确认；注释提及不计） |
| A5 | 静态 | 聊天请求不再自拼地址 | api.js 搜 `localhost` 关键字 | 代码中仅 API_BASE_URL（[L11](src/services/api.js#L11)，axios baseURL 配置来源）1 处命中；头部规范注释内作为示例出现的 URL 不计入 |
| A6 | 静态 | 规范注释存在且完整 | api.js 头部块注释截图 | 包含「正确用法/禁止写法/配置位置/新增模板」四要素 |
| A7 | 静态 | 代码减量达标 | `git diff --stat main...HEAD` 输出（PR #3 实测 68+/114-，含 commit 4 评审修正） | Deletions(-) - Insertions(+) ≥ 40 行（单文件 api.js；其中 Commit 2 纯删除 74 行） |
| B1 | 运行时 | 登录请求等价性 | DevTools Network 登录请求 Before/After 对比截图 | URL/Method/Content-Type/Body 字节级一致；错误文案语义一致 |
| B2 | 运行时 | 测评提交请求等价性 | 同上，测评提交请求截图 | 同上 |
| B3 | 运行时 | AI 聊天请求等价性（核心变更点） | 同上，/api/coach/chat 请求截图 + Console 错误日志（无 Key 场景） | URL 继承 axios baseURL（不再自拼 localhost）；JSON Body 结构完全等价；**成功判定保留 `result.status === 'success'` 包络判断（不依赖 HTTP 200）**；HTTP 400/500 时 catch 抛出的 Error 文案与旧实现语义一致 |

> **范围口径声明（2026-09-06 评审意见闭环后补充）**：本 PR 的 A1~A7 验收口径一律为「**src/services/api.js 代码内、不含注释**」（A2/A5 的原始定义）。全仓范围内另有范围外残留，均不在本 PR diff（Files changed: 1）内，不推翻「api.js 单文件收敛」结论：
> - `src/utils/testApi.js`（dev 调试工具，被 [index.js](src/index.js#L7) 副作用引入；靠原生 fetch 探测 CORS 响应头属刻意设计）：保留 3 处 `fetch(` + `http://localhost:5001`
> - `src/setupProxy.js`（CRA dev-server 代理，开发环境基建）：保留 `http://localhost:5001`
> - 跟进项：与 Q3 的 STAGE3-ENG-003（husky + `fetch(` 提交检查）一并列入阶段 3 Backlog，届时决定 testApi.js 迁移或下线。

### 5.2 风险识别与缓解预案

| 风险编号 | 风险描述（发生概率 × 影响范围） | 触发条件 | 缓解预案（合并前执行） |
|---|---|---|---|
| R1 | sendMessageToCoach 错误文案不一致（P=低 × I=低） | axios 原生 HTTP 4xx/5xx 错误 message 为英文 `Request failed with status code 400`；上游响应拦截器直接 reject 原始 error（L36-L48，不包装），error.response 原样保留 | Commit 1 模板的 catch **优先取后端响应体中文 message**（error.response.data.message；后端 400/500 均带 {status:'error', message}，见 coach_routes.py L50-L76），仅当后端漏写 message 时才兜底；验收 B3 截图比对 Console 文案语义 1:1 |
| R2 | 返回值层级错位 / 包络判断缺失（P=中 × I=高） | ① 错写成 `return { reply: response.reply }`（漏 response.data）→ reply 为 undefined；② 漏掉 `result.status === 'success'` 判断 → 后端 200 + {status:'error'} 时被误当成功（reply 也为 undefined） | Commit 1 模板**已写死两层正确写法：`const result = response.data; if (result.status === 'success') return { reply: result.reply };`**；本地运行时 Console.log(response) 确认层级，打勾 B3 时强制检查 reply 非 undefined |
| R3 | 误删 fetchApi 相邻代码（P=低 × I=高） | Commit 2 删除 fetchApi 时边界判断错误，误删上方响应拦截器（L36-L48）或下方 registerUser（L125 起），导致 build 报错 | 删除锚点严格按 L50-L123（`// 通用请求函数` 注释行起、函数闭合 `}` 止）；删除后立刻 `git grep fetchApi` + `npm run build` 验证（A1 前置），发现报错立刻撤销重选范围 |
| R4 | sendMessageToCoach 迁移后行为回归不充分（P=低 × I=中） | 聊天从「内联 fetch」切到 axios 实例后（正式 main 的 axios 无超时/重试配置，行为差异仅在错误对象形态），若只验证成功路径，包络分支 / 400 文案分支未覆盖 | 按 B3 用例完整回归：成功包络、HTTP 500（无 Key 场景）、可选 HTTP 200 + {status:'error'} mock 三种分支都要 Console 观察通过 |
| R5 | 未来新代码再写 fetch 封装（P=中 × I=低） | fetchApi 从未 export、当前外部 0 引用，协同学旧分支无法依赖它（构建期即报错）；残余风险是新人另起炉灶新写 fetch 封装 | Commit 3 规范注释 + PR 合并后 Group 群内通知：「api.js 已统一走 axios 实例，fetchApi 已移除，禁止新写 fetch( / 新的 fetch 封装」 |

### 5.3 回滚方案（任何异常发生时 2 分钟可还原）

**原则：先回滚止血，再定位根因复现修复，不要在线上环境 debug。**

#### 场景 1：PR 合并前（Code Review 阶段）发现问题 —— 最简单
→ 直接在 GitHub PR 页面点「Close Pull Request」，不合并即可。
→ 本地分支保留用于调试，问题修复后重新 push 再开 PR。

#### 场景 2：PR 已合并到 main 但线上功能报错（如 AI 聊天回复为空）—— 3 条命令回滚
```powershell
# S1 切回 main 并拉取最新（确保 HEAD=有问题的合并提交）
git checkout main
git pull origin main

# S2 找到问题 PR 的 Merge Commit Hash（记为 abc1234）
git log --oneline -n 10
# 输出：abc1234 Merge pull request #X from XiaoCow666/refactor/api-request-unify  ← 记住这个 hash

# S3 回滚该 Merge Commit（-m 1 表示保留 main 主线作为回滚后的父节点）
git revert -m 1 abc1234
# 会自动生成一个 "Revert refactor/api-request-unify" 的新 Commit，代码恢复到 PR 合并前状态

# S4 验证无误后推回远端 main（立刻止血）
git push origin main

# S5 基于原 refactor/api-request-unify 分支 debug，问题定位后重新开 PR2 提交
```

#### 场景 3：Merge Commit 无法回滚（极少数情况）
→ 退化为「基于文件级别的精准还原」：
```powershell
# 用问题 PR 合并前的 main 版本覆盖 api.js（本 PR 仅改动 1 个文件）
git checkout <PR前稳定commit> -- src/services/api.js
git commit -m "hotfix: 手动回滚 STAGE2-ISSUE-001 api.js 改动"
git push origin main
```

#### 回滚后验证清单（必做，防止"回滚没真回滚"）：
- [ ] api.js 搜 `fetchApi` → 出现定义（L50-L123，74 行）= 回滚成功
- [ ] api.js 搜 `sendMessageToCoach` 函数内部 → 能看到旧实现内联 `fetch(url, ...)` 拼接 `/api/coach/chat`（L185-L235）= 回滚成功
- [ ] `npm run build` 通过 = 回滚成功

---

## 六、仍需确认的问题（STAGE2-ISSUE-001 实施前必须在团队群/评审会上拍板）

| 编号 | 问题 | 背景 | 对改动影响等级 | 建议默认决策 | 确认人 | 截止日期 |
|---|---|---|---|---|---|---|
| Q1 | ~~是否确认 fetchApi 在所有人的本地分支中都没有被调用？~~ → **已核对，关闭**：fetchApi 未 export（任何外部 import 都会构建报错），正式 main 上 src 内 git grep 仅命中定义行 L51，0 调用 | 原风险「协同学分支依赖 fetchApi 造成 merge 冲突」不成立 | ⚪ 无（已关闭） | **无需群公告**：PR #3 描述已注明「fetchApi 未导出、0 引用」；合并后群内仍发 1 条「禁止新写 fetch 封装」通知（见风险 R5） | 技术负责人 | 2026-09-05 已核对 |
| Q2 | axios 实例是否在本次补加 timeout / 超时重试等「统一配置」？ | 正式 main（930da2c）的 axios 实例只有 baseURL + headers（L14-L19），拦截器仅处理 JWT 与 401，**无 timeout / 超时重试机制**（工作区演进版另有实现，不属于本 PR 范围）；本 PR 只做链路收敛 | 🟡 低（额外加分项，不影响本方案目标） | **默认「本次不加，留到阶段 3 新 PR」**：本 PR 范围已写死为"收敛统一"，关注点分离。阶段 3 单独 PR `feat(api): axios 加 timeout + 超时重试/统一错误文案拦截器` | 技术负责人 | 阶段 3 启动时 |
| Q3 | 是否需要在 CI 里加一条「禁止新写 fetch( / 新 fetch 封装」的自动检查规则？（防止半年后又有人重蹈覆辙） | 本地团队成员规范依赖自觉性，可能出现新人不知道本 PR 的历史背景又犯同样错误。可以用 husky + lint-staged + 简单 grep 脚本实现「提交时检测到 src 中出现 fetch( 就拒绝提交，并打印本方案 Q3 链接指引迁移」。当前项目未接入 husky。 | 🟡 低（属于工程化建设长期项） | **默认「本方案暂不实现，列入阶段 3 Backlog」**：理由同 Q2，避免 PR 范围膨胀。阶段 3 独立 Issue `STAGE3-ENG-003 接入 husky + 请求工具规范 pre-commit 检查`，估算工期 0.5 天 | 工程化负责人 | 阶段 3 启动时 |
| Q4 | sendMessageToCoach 错误文案的中文来源需要确认吗？（风险 R1） | 对照基准已存在且无需产品介入：后端教练接口 400/500 响应体均带中文 {status:'error', message}（[coach_routes.py#L50-L76](backend/app/routes/coach_routes.py#L50-L76)），旧 fetch 链路就是原样取它并加「AI教练回复错误:」前缀 | 🟡 低（直接影响验收 B3 文案语义） | **默认「无需产品出文案表」**：Commit 1 模板 catch 优先取后端 response.data.message（中文 1:1 对齐旧版）；仅当后端某分支漏写 message 时才走兜底通用文案。验收 B3 截图比对 Console 文案 | 前端负责人 | 2026-09-07 PR 合并前 |
| Q5 | 本 PR 的 Code Reviewer 指派给谁？（避免 PR 长期挂着无人审） | 当前项目无明确 Code Review 分工约定 | 🟡 低（协作流程问题） | **默认指派「本方案分析人 / 前端编写主力 + 1 名协同学交叉评审」**：2 人审完即可合并；如 2 个工作日内无 Review 意见，按「过期自动通过」规则处理 | 技术负责人在项目看板中指派 | 2026-09-04 前 |

---

## 附录：判断与推荐总结（3~5 句话版本）

> 本方案选择「将聊天请求 sendMessageToCoach 从内联 fetch 收敛到 axios.create 实例，并删除 fetchApi 原生 fetch 封装死代码（74 行）」作为阶段 2 练手问题，原因是：改动范围集中（仅 api.js 1 个文件）、风险低（函数签名与返回值结构不变、调用方 CoachChat.js#L216 0 改动、业务包络判断与错误文案 1:1 保留）、工期半天可完成，且能同时解决「聊天请求绕过 axios 的 baseURL 造成部署必现跨域」与「fetchApi 死代码 + 内联 fetch 双份重复实现」两个真实痛点。方案设计 4 个 Conventional Commits（迁移 → 删除 → 规范注释 → 评审修正注释示例），便于 Code Review 和分 commit 回滚；验收标准 A1~A7 静态 + B1~B3 运行时共 10 项，可操作性强。方案 1 与方案 2 的代码动作仅差一份规范注释，30min 边际成本补齐 G5，综合边际 ROI 最高。**代码已实施并开 PR：XiaoCow666/Caifusi#3（fairy-song:Caifusi-1:refactor/api-request-unify → main，68+/114- 净删 46 行，含评审修正 commit `c8017ea`），请按 PR 复审。**

---

**文档末尾元数据**：
- 本文档为评审修订稿（原始文档：`阶段2-改进方案-api请求统一.md`）；正文证据与行号以**正式 main = XiaoCow666/Caifusi@930da2c** 为准
- **修订记录（2026-09-05）**：
  ① 修正 axios 路径拼写：baseURL 已含 /api（API_BASE_URL L8-L11），请求 path 一律不带 /api（`api.post('/coach/chat')`，不能写 `/api/coach/chat`，否则得 /api/api/coach/chat）；
  ② 按正式 main 重核 fetchApi：**0 调用死代码（74 行，未导出）**，评审早期结论成立；注：工作区演进版分支线（fairy-song/CaiFuSi:main 9838f04 及其后 commit：超时/重试工具链、fetchApi 被单点调用等）与正式 main 已分叉，**不在本 PR 范围**；
  ③ sendMessageToCoach 重写模板**保留 `result.status === 'success'` 业务包络判断**（旧实现 L221 已有）：不能仅以 HTTP 200 判定成功；400/500 错误文案优先取后端响应体中文 message；
  ④ 代码已实施并开 PR：**PR #3**（https://github.com/XiaoCow666/Caifusi/pull/3）——commit a15d154（迁移 24+/40-）+ 8988d06（删死代码 -74）+ 8abc0b3（规范注释 +41），合计 65+/114-，净删 49 行，每个 commit 均 `npm run build` 通过；
  ⑤ （2026-09-06，评审意见闭环）两条复审意见均属实并已修复：
     a) **api.js 顶部示例 import 了未导出的 named export `api`**（8abc0b3 头部注释）——`api` 为模块私有 axios.create 常量，仅具名封装函数与 `export default apiService` 可导入，照抄示例会构建报错，违背 G5。已由 **commit 4 `c8017ea`（fix(api): 修正顶部规范注释示例——api 实例未导出，外部不得 import { api }）**修正：示例改用具名导出 / 默认导出 apiService，并注明实例仅供文件内部使用；同步修正 §4.3 Step 3-1 模板。
     b) **`src/utils/testApi.js` 保留 3 处 `fetch(` + `http://localhost:5001`**（基线 930da2c 即存在、PR diff 未触及；另有 setupProxy.js 的 dev proxy 地址），故 PR 描述中「全仓 0 命中 / 仅剩 1 处」类措辞不成立。已收口：PR 描述与 §5.1 验收口径统一为「**src/services/api.js 代码内、不含注释**」（即 A2/A5 原始定义），testApi.js / setupProxy.js 列为范围外残留并记入阶段 3 跟进（见 §5.1 范围口径声明）。
     修订后 PR 全量统计：**68+/114-，净删 46 行**（单文件仍仅 api.js）。
- 对应问题代码锚点（正式 main 930da2c）：[api.js#L14-L19](src/services/api.js#L14-L19)（axios 实例配置，baseURL 来源 [L8-L11](src/services/api.js#L8-L11)）、[api.js#L50-L123](src/services/api.js#L50-L123)（fetchApi 死代码 74 行）、[api.js#L185-L235](src/services/api.js#L185-L235)（sendMessageToCoach 内联 fetch）、[CoachChat.js#L216](src/pages/CoachChat.js#L216)（调用方，0 改动）
- 下次 Review 节点：分支切出 + 第一个 Commit 提交后约 4 小时做首次同步