# Flask 框架及扩展
flask>=3.0.0
flask-cors>=4.0.0

# 环境变量管理
python-dotenv>=1.0.0

# HTTP 请求库
requests>=2.31.0

# 智谱 AI SDK
zhipuai>=2.1.5
# zhipuai SDK 运行时依赖（显式声明，避免隐式依赖缺失）
sniffio>=1.3.0

# MySQL 数据库驱动及加密支持
PyMySQL>=1.1.0
cryptography>=41.0.0

# --- 可选依赖（按需单独安装） ---
# Firebase Admin SDK → 见 requirements-firebase.txt
# Google Generative AI (Gemini) → 见 requirements-gemini.txt
# WSGI 服务器 gunicorn（Unix 生产环境）→ 见 requirements-prod.txt
