# 开发环境变量（跨平台兼容）
# 这些变量原硬编码在 package.json start 脚本的 Windows CMD `set` 语法中，
# macOS/Linux 下 `npm start` 会因 `set` 不是 bash 命令而失败。
# 移至 .env.development 后由 react-scripts（dotenv）在所有平台统一加载。

# 开发服务器监听地址（允许局域网内其他设备访问）
HOST=0.0.0.0

# 禁用 Host 检查（配合局域网访问，防止 Invalid Host header）
DANGEROUSLY_DISABLE_HOST_CHECK=true

# HMR WebSocket 连接主机
WDS_SOCKET_HOST=localhost
