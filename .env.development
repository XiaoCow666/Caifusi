# 开发环境变量（跨平台兼容）
# 这些变量原硬编码在 package.json start 脚本的 Windows CMD `set` 语法中。
# 注意：bash 的 `set` 是内置命令，但语义是设置 shell 选项，不会按 CMD 语义导出环境变量。
# 移至 .env.development 后由 react-scripts（dotenv）在所有平台统一加载。

# 开发服务器监听地址。默认仅本机（localhost）。
# 安全说明：不要默认改为 0.0.0.0 —— 监听所有网卡会向局域网暴露开发服务器，
# 配合禁用 Host 校验还会引入 DNS rebinding 风险。需要局域网访问时，
# 显式设置为 HOST=0.0.0.0，并保持 DANGEROUSLY_DISABLE_HOST_CHECK 不设置。
HOST=localhost

# HMR WebSocket 连接主机
WDS_SOCKET_HOST=localhost
