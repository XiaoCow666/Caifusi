# Caifusi 接管演练报告

**日期**：2026-09-11
**基线**：`XiaoCow666/Caifusi@c4bdb94`（PR #19 合并后的 main）
**变更分支**：`fairy-song/Caifusi-1:fix/router-hash-redirect`（本报告随该 PR 提交）

## 一、演练目的与边界

接管演练＝模拟一名新维护者，只依据仓库自带文档，从零复现项目、判断当前状态，并把过程中发现的缺陷固定成可复核的证据。

- **本次做**：全新克隆 → 按 README / DEPLOYMENT_GUIDE.md 安装并启动前后端 → 跑测试与构建 → 端到端复核首屏跳转与站内导航 → 修复两处导航缺陷并补测试。
- **本次不做**（高风险边界，仅记录证据、留给维护者决策）：不修改数据库结构、权限、安全策略、部署架构与破坏性接口；**未重新生成 `docs/` 下已提交的构建产物**（理由见 §四 F3）。

## 二、环境

| 项 | 值 |
| --- | --- |
| 操作系统 | Windows 11 |
| Node / npm | v24.18.1 / 11.16.0 |
| Python | 3.14.6（`D:\python\python.exe`） |
| 克隆方式 | `git clone --depth 1 https://github.com/XiaoCow666/Caifusi.git`（全新目录，非既有工作区） |
| 克隆后 HEAD | `c4bdb94 fix: 后端启动依赖优化与前端构建修复（阶段二） (#19)` |

本机 Python 环境**没有** `pymysql`（`ModuleNotFoundError`），其余依赖（flask 3.1.3 / flask-cors 6.0.5 / python-dotenv / requests / zhipuai 2.1.5 / sniffio 1.3.1 / cryptography）均已就绪——这正好构成对 PR #19「memory 模式不硬依赖 pymysql」的独立复核条件。

## 三、复现步骤与结果

| # | 命令 | 结果 |
| --- | --- | --- |
| 1 | `npm ci --no-audit --no-fund` | exit 0 |
| 2 | `CI=true npm test -- --watchAll=false` | exit 0；1 suite / **31 passed**（1.9s） |
| 3 | `CI=true npm run build` | exit 0；`Compiled successfully`；产物 `main.319cba4e.js` 497.28 kB、`main.b4272f3b.css` 43.29 kB |
| 4 | 按 README 建 `.env`（占位值）后 `python backend/run_dev_enhanced.py` | 启动成功；`/api/health` → 200 `{"status":"healthy"}`；`/api/coach/health` → 200；`/api/assessment/latest` → 200 `{"assessment":null}` |
| 5 | `npm start` | 编译成功；`http://localhost:3000/` → 200，`<title>财赋思 - AI金融心智教练</title>` |

**对 PR #19 的独立复核**：第 3 步产物文件名/体积与其正文声称完全一致；第 4 步在**未安装 `pymysql`** 的环境下后端仍能启动并返回 200，与其「延迟导入、memory 模式不硬依赖」的描述一致。两项声称均可复核。

## 四、发现

### F1（已修复）HashRouter 首屏重定向会把子路径部署的用户跳出应用

`src/App.js` 的 `RedirectHandler` 原实现：

```js
if (location.pathname === '/' && !location.hash) {
  window.location.replace('/#/');
}
```

`src/index.js` 使用 `<HashRouter>`，地址栏才是路由来源。实测（在 `HashRouter` 内渲染探针组件，地址栏置为 `#/`）：

```
地址栏 hash = "#/"
useLocation() = {"pathname":"/","search":"","hash":"",...}   ← hash 恒为空串
```

即 `#/` 与「完全没有 hash」两种地址栏在路由 `location` 上不可区分，于是：

1. **跳转目标写死 `'/#/'`（站点根）**：`DEPLOYMENT_GUIDE.md` 明确要求按 `PUBLIC_URL=.` 部署到 `/仓库名/` 子路径，此时该跳转会把用户从 `/Caifusi/` 导航到站点根 → 应用被甩出。
2. 判断条件读错字段：地址栏已是 `#/` 时仍判定为「没有 hash」，对同一 URL 重复发起 `replace`（同文档内跳转，不会重载，但逻辑恒真）。

**端到端复核**（无头 Edge + 最小静态服务器把构建挂在 `/Caifusi/`，站点根返回 `BOUNCED-TO-ROOT` 标记页）：

| 版本 | 请求轨迹 | 最终 DOM |
| --- | --- | --- |
| 基线 `c4bdb94` | `GET /Caifusi/` → 静态资源 → **`GET /`** | `BOUNCED-TO-ROOT`（应用已被甩出） |
| 本分支 | `GET /Caifusi/` → 静态资源（**无 `GET /`**） | 应用正常渲染；最终 URL 停在 `/Caifusi/…#/` |

### F2（已修复）法务页「联系客服」用绝对路径 `<a href="/info/contact">`

`src/pages/info/LegalPage.js:399`。整页跳转 + 子路径部署下请求落到站点外 → 404；站内导航应使用 react-router 的 `<Link>`。这是 `src/` 下唯一一处站内绝对链接（已全量扫描确认）。

### F3（仅记录证据，未修改）线上 Pages 与已提交构建产物的状态

- 实测 `https://xiaocow666.github.io/Caifusi/` 与 `https://xiaocow666.github.io/` 均返回 **404**；`GET /repos/XiaoCow666/Caifusi/pages` 返回 **404 Not Found**（2026-09-11 实测）→ 该仓库当前**没有启用 GitHub Pages**（事实）；未启用的原因不在本次演练可确认范围内（推断边界）。
- `docs/` 下已提交的构建产物最后一次变更是提交 `1be65bb`（"调整项目结构以适应GitHub Pages部署"），早于后续 AI 教练等功能；其 `docs/static/js/main.94991b0d.js` 中检索不到 `sendMessageToCoach` / `5001` / `'/#/'` 等后续实现字样 → **与当前源码不同步**（事实）。
- 未重新生成并提交 `docs/`：属于部署范畴且会引入大体量产物改动，按本次边界交给维护者决策。

## 五、本次修复与验证

| 文件 | 变更 |
| --- | --- |
| `src/utils/routeRedirect.js` | 新增：把「是否需要补全 hash 根路由、补成什么 URL」抽成纯函数 `resolveRedirectTarget(routerPathname, windowLocation)` |
| `src/App.js` | `RedirectHandler` 改调该函数：判断读 `window.location.hash`，跳转目标保留当前路径前缀 |
| `src/pages/info/LegalPage.js` | 「联系客服」改 `<Link to="/info/contact">` |
| `src/utils/routeRedirect.test.js` | 新增 6 条用例：`#/` 不再重定向、`/Caifusi/` → `/Caifusi/#/`、本地 `/` → `/#/`、保留查询串、非根路由不跳转；另加「`src` 下不得出现站内绝对 `href`」契约扫描 |

验证命令与结果（修复分支工作区）：

```
$ CI=true npm test -- --watchAll=false
Test Suites: 2 passed, 2 total
Tests:       37 passed, 37 total        # 31 基线 + 6 新增

$ MSYS_NO_PATHCONV=1 PUBLIC_URL=/Caifusi CI=true npm run build
Compiled successfully.   exit 0         # lint 门禁（CI 模式）同样通过
```

## 六、事实与推断边界

- 报告中带引号的日志、状态码、产物名与体积均为本次实测输出；命令见 §三、§五。
- §四 F1 的「跳出应用」由无头浏览器 + 本地静态服务器复现，为**受控环境**结论；线上 Pages 当前未启用（F3），无法在真实站点上复现。
- §四 F3 的「产物不同步」是**静态比对**结论（在已提交 bundle 中检索后续实现字样）；未逐文件比对构建与源码的完整差异。
- 本报告未覆盖：后端业务逻辑正确性、数据库/鉴权/安全策略、生产部署链路——这些不在本次演练范围。
