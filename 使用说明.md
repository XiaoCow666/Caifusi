# 财赋思应用 - 使用说明

## 🚀 快速启动

### 一键启动（推荐）

**直接双击运行：`快速启动.cmd`**

脚本会自动：
1. ✓ 检测Node.js和Python环境
2. ✓ 安装必要的依赖（express@4, node-fetch@2）
3. ✓ 启动后端服务（Flask on 5001）
4. ✓ 启动前端服务（Express on 3000）
5. ✓ 自动打开浏览器访问应用

### 访问地址

- **前端页面**: http://localhost:3000
- **后端API**: http://localhost:5001

## 📋 环境要求

### 必需软件

1. **Node.js** (推荐 v18.x 或更高)
   - 下载地址: https://nodejs.org/
   - 安装LTS版本

2. **Python** (3.8 或更高)
   - 下载地址: https://www.python.org/
   - 安装时勾选"Add Python to PATH"

### 依赖包

**前端依赖**（自动安装）：
- express@4.x
- node-fetch@2.x

**后端依赖**（需手动安装）：
```bash
python -m pip install -r backend/requirements.txt
```
> `requirements.txt` 为核心依赖。如需 Firebase 存储或 Gemini 模型，追加对应可选文件：`python -m pip install -r backend/requirements.txt -r backend/requirements-firebase.txt` 或 `python -m pip install -r backend/requirements.txt -r backend/requirements-gemini.txt`。本地开发不需要安装 `requirements-prod.txt`（gunicorn，仅 Unix 生产环境）。

## 🔧 项目结构

```
CaiFuSi/
├── 快速启动.cmd              # 一键启动脚本
├── simple-static-server.js   # 前端静态服务器
├── package.json              # 前端依赖配置
├── backend/                  # 后端代码
│   ├── run_dev_enhanced.py  # 后端启动脚本
│   ├── requirements.txt     # Python依赖
│   └── app/                 # Flask应用
├── src/                      # React源代码
├── build/                    # 前端构建文件
└── docs/                     # 文档

文档文件：
├── 2025大创答辩.md           # 答辩准备材料
├── 财赋思-大创答辩稿.md      # 答辩讲稿
├── 财赋思-6大功能技术讲解稿.md # 功能技术讲解
└── README.md                 # 项目说明
```

## ⚙️ 配置说明

### 环境变量配置

后端配置放在仓库根目录的 `.env` 中。可以先复制示例文件：

```powershell
Copy-Item .env.example .env
```

然后在 `.env` 中填写后端需要的值：

```env
# 后端密钥：不要提交真实值
ZHIPUAI_API_KEY=your_zhipuai_api_key_here
SECRET_KEY=replace_with_a_random_secret
# 可选
GEMINI_API_KEY=your_gemini_api_key_here
```

前端 Firebase 配置才使用 `.env.local`：

```env
REACT_APP_FIREBASE_API_KEY=your_firebase_key
REACT_APP_FIREBASE_AUTH_DOMAIN=your_domain
REACT_APP_FIREBASE_PROJECT_ID=your_project_id
REACT_APP_FIREBASE_STORAGE_BUCKET=your_storage_bucket
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
REACT_APP_FIREBASE_APP_ID=your_app_id
REACT_APP_FIREBASE_MEASUREMENT_ID=your_measurement_id
```

不要把 API Key 写入 `backend/app/services/config.py` 或其他源代码文件。后端启动脚本会读取根目录 `.env`，应用配置从环境变量读取；真实密钥也不要提交到 Git。

## 🛠️ 常见问题

### Q1: 提示"未找到Node.js"

**解决方案**：
1. 确认已安装Node.js
2. 脚本会自动搜索常见安装路径
3. 如果仍然失败，手动添加Node.js到系统PATH

### Q2: 前端无法启动

**解决方案**：
```bash
# 手动安装前端依赖
npm install

# 或者使用完整路径运行
"C:\Program Files\nodejs\node.exe" simple-static-server.js 3000 5001
```

### Q3: 后端无法启动

**解决方案**：
```bash
cd backend
python -m pip install -r requirements.txt
python run_dev_enhanced.py
```

### Q4: 端口被占用

**解决方案**：
```bash
# 查看端口占用
netstat -ano | findstr "3000"
netstat -ano | findstr "5001"

# 结束占用进程
taskkill /F /PID <进程ID>
```

### Q5: AI功能不可用

**解决方案**：
1. 检查智谱AI API密钥是否正确配置
2. 确认API密钥有效且有余额
3. 查看后端窗口的错误日志

## 📊 技术栈

### 前端
- React 18.2.0
- React Router 6.30.1
- Bootstrap 5.3.6
- Firebase 11.8.1
- Axios 1.3.4

### 后端
- Python Flask 3.x（版本范围见 `backend/requirements.txt`）
- 智谱AI GLM-4
- Firebase Admin SDK
- Flask-CORS

### 服务器
- Express 4.x（静态文件服务）
- Node-fetch 2.x（API代理）

## 🎯 开发模式

### 前端开发

```bash
# 启动React开发服务器
npm start

# 构建生产版本
npm run build
```

### 后端开发

```bash
cd backend
python run_dev_enhanced.py
```

## 📝 注意事项

1. **首次启动**：AI模型初始化需要1-2分钟
2. **网络要求**：需要访问智谱AI和Firebase服务
3. **端口占用**：确保3000和5001端口未被占用
4. **依赖版本**：使用express@4而非express@5（兼容性更好）

## 🆘 获取帮助

如果遇到问题：
1. 查看启动窗口的错误日志
2. 检查环境变量配置
3. 确认所有依赖已正确安装
4. 参考`docs/`文件夹中的详细文档

## 📄 许可证

本项目采用MIT许可证。详见 [LICENSE](LICENSE) 文件。

---

**祝使用愉快！** 🎉
